@{
    ViewData["Title"] = "Search by name";
}

<div class="text-center">
    <h1 class="display-4">Enter a Name</h1>
    <p>
        Name: <input type="text" name="txtName" id="txtName" placeholder="Gyarados"><br>
        <input type = "submit" id = "submit" name = "submit" value = "Search" onclick=" location.href = ' @Url.Action("PokemonDisplay", "Home" )?name=' + document.getElementById('txtName').value " />
    </p>
</div>

<!--
alert(document.getElementById('txtName').value);
  


@using (Html.BeginForm("PokemonDisplay", "Home")){
          <input type = "submit" value = "Search" />
        }
        <input type="button" value="Detail" onclick="location.href='@Url.Action("Detail", "Home")?id=' + $('#Id_Of_Textbox').val()" />
        <input type="button" value="Detail" onclick="location.href='@Url.Action("Detail", "Home")?id=' + document.getElementById('Id_Of_Textbox').value" />
        <input type = "button" value = "Search" onclick = " location.href = ' @Url.Action("PokemonDisplay", "Home" ) ' " />

  -->